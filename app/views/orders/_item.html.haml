-@order.meals.items.each do |i|
  %div{class: 'media media-stylish', id: "item#{i.id}"}
    %div{class: 'media-middle col-md-6'}
      %h5{class: 'media-heading'}
        =i.name
      %p
        =i.description
    %div{class: 'media-right col-md-3'}
      %h5
        ='$ ' + i.price.to_s + '0'
